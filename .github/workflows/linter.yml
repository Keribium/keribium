name: linter

on:
  push:
    branches: [main]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  super-linter:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Super-Linter
      uses: github/super-linter@v4.10.1
